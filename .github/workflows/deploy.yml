name: Update Directory Structure
on:
  push:
    branches:
      - main
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: test
        env:
          API_KEY: ${{ secrets.FIREBASE_API_KEY }}
        run: |
          echo $API_KEY
